# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.*
!.env.example

# Uploaded files (local dev only — production uses Cloudinary)
uploads/*.jpg
uploads/*.jpeg
uploads/*.png
uploads/*.webp
uploads/*.gif

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db
